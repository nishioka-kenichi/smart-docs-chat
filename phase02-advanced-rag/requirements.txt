# Core dependencies
langchain==0.3.0
langchain-openai==0.2.0
langchain-community==0.3.0
langchain-chroma==0.1.4
chromadb==0.5.0

# LLM and embeddings
openai==1.51.0

# ============================================
# Reranker components (optional)
# ============================================
# Rerankerはオプショナルです。必要に応じてインストール：
#
# Cross-Encoder Reranker（ローカル実行、無料、精度向上）
sentence-transformers==3.0.0
torch>=2.0.0  # sentence-transformersの依存
#
# 注意: Rerankerなしでも動作します（SimpleRerankerが自動使用）

# Utilities
python-dotenv==1.0.0
pyyaml>=6.0
numpy>=1.26.0
pandas>=2.0.0
tqdm>=4.66.0

# Development
pytest>=8.0.0
ipykernel>=6.29.0
debugpy>=1.8.0
# jupyter==1.0.0

# Logging
loguru==0.7.2